Iqm.bat is a shorthand for direct conversions. To use it, simply drag your model file into the batch file.

Note: only FBX, OBJ, SMD, MD5, IQE and IQM (version 1) are supported.

DirectCMD.bat automatically sets the command path to this folder; permitting to directly access applications in command line.